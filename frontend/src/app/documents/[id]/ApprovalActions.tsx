/**
 * ApprovalActions — DEPRECATED / DEAD CODE (C1, POR-147 / ARU-17-C1)
 *
 * This component has been superseded by PackageDetailActions.tsx.
 * It contains v0.1 copy ("Approve", "Reject") that violates spec §1.1.
 *
 * This file is intentionally empty — the component was removed as part of
 * dead code cleanup in C1. The file itself cannot be deleted via the agent
 * toolchain; the dev or CI can `git rm` it during the merge step.
 *
 * Replacement: @/app/documents/[id]/PackageDetailActions
 */

export {};
